# HomelabAiSlopUpdater

Just an AI-Slop created by cursor.
Works for me because I'm lazy and just want to have an easy way to update my homelab 

Do whatever you want with it. 

Pretty sure no more updates because I just needed something to run updates on many VMs :D
Spend more time writing this than actually coding

# How it works:

- Just opens a Window where you can create new machines with commands.
- Saves them into a sqlite database
- Then you can run the saved command
- Results are saved in the database
